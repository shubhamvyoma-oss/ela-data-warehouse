# Monitoring service

Reserved for the independently deployable monitoring service. Current health state is modeled in
the `monitoring` schema; no service implementation is fabricated before its contract is approved.
