# Phase 1 has no platform-level environment variables.
#
# Each service manages its own environment configuration.
# For the current webhook service, copy and configure:
#
#   services/edmingle_webhook/.env.example
