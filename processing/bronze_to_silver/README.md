# Bronze to Silver

This boundary owns typed parsing, normalization, identity resolution, data-quality validation, deduplication, and quarantining. Nothing is implemented from assumptions: each model requires an approved source contract and target grain.
